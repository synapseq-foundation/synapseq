## 1. README Examples

- [x] 1.1 Add a compact `.spsq` example to `README.md` that shows options, one preset with indented tracks, and timeline entries.
- [x] 1.2 Add a compact Go `spsq.Builder` example to `README.md` that uses `spsq.New()`, `NewPreset`, a track builder call, timeline construction, and `Load(ctx)`.
- [x] 1.3 Keep links to `docs/SYNTAX.md`, `core`, and `spsq` Go API documentation visible near the examples.

## 2. Verification

- [x] 2.1 Review the `.spsq` example against the current parser syntax and sequence builder rules.
- [x] 2.2 Review the Go builder example against the current public `spsq` API.
- [x] 2.3 Run relevant documentation/example checks if any executable Go example is added; otherwise verify README rendering manually.
